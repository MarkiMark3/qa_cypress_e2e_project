/// <reference types='cypress' />
/// <reference types='../support' />

describe('User', () => {
  before(() => {
    cy.task('db:clear');
  });

  it('should be able to follow the another user', () => {

  });
});
